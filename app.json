{
  "name": "github-delete-old-branches",
  "description": "Periodically delete old branches on GitHub.",
  "repository": "https://github.com/ezyang/github-delete-old-branches",
  "keywords": ["github"],
  "env": {
      "ACCESS_TOKEN": {
          "description": "An access token with rights to delete branches",
          "required": true
      },
      "REPO": {
          "description": "Repository to prune commits from",
          "required": true
      },
      "RETENTION_DAYS": {
          "description": "How old a branch must be before pruning",
          "value": "21"
      }
  }
}
